{{ header }}{{ column_left }}
<div id="content">
  <div class="page-header">
    <div class="container-fluid">
      <div class="float-end">
        <button type="submit" form="form-filterpro" data-bs-toggle="tooltip" title="{{ button_save }}" class="btn btn-primary"><i class="fa-solid fa-floppy-disk"></i></button>
        <a href="{{ back }}" data-bs-toggle="tooltip" title="{{ button_back }}" class="btn btn-light"><i class="fa-solid fa-reply"></i></a>
      </div>
      <h1>{{ heading_title }}</h1>
      <ol class="breadcrumb">
        {% for breadcrumb in breadcrumbs %}
          <li class="breadcrumb-item"><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
        {% endfor %}
      </ol>
    </div>
  </div>

  <div class="container-fluid">
    <div class="card">
      <div class="card-header"><i class="fa-solid fa-filter"></i> {{ text_edit }}</div>
      <div class="card-body">
        <form id="form-filterpro" action="{{ save }}" method="post" data-oc-toggle="ajax">
          <div class="row mb-3 required">
            <label for="input-name" class="col-sm-2 col-form-label">{{ entry_name }}</label>
            <div class="col-sm-10">
              <input type="text" name="name" value="{{ name }}" placeholder="{{ entry_name }}" id="input-name" class="form-control"/>
              <div id="error-name" class="invalid-feedback"></div>
            </div>
          </div>

          <div class="row mb-3">
            <label for="input-status" class="col-sm-2 col-form-label">{{ entry_status }}</label>
            <div class="col-sm-10">
              <select name="status" id="input-status" class="form-select">
                <option value="1" {% if status %}selected{% endif %}>{{ text_enabled }}</option>
                <option value="0" {% if not status %}selected{% endif %}>{{ text_disabled }}</option>
              </select>
            </div>
          </div>

          <div class="row mb-3">
            <label class="col-sm-2 col-form-label">{{ entry_use_globally }}</label>
            <div class="col-sm-10">
              <div class="form-check form-switch">
                <input class="form-check-input" type="checkbox" name="use_globally" value="1" id="input-use-globally" {% if use_globally %}checked{% endif %} {% if not module_id %}disabled{% endif %}>
                <label class="form-check-label" for="input-use-globally"></label>
              </div>
              {% if not module_id %}
                <div class="form-text">{{ text_save_first|default('Save module first to enable global binding.') }}</div>
              {% endif %}
            </div>
          </div>

          <hr/>

          <h3 class="mb-3">{{ text_blocks|default('Blocks') }}</h3>

          <div class="table-responsive">
            <table class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>{{ column_block }}</th>
                  <th style="width:180px">{{ column_display }}</th>
                  <th style="width:120px">{{ column_expanded }}</th>
                  <th>{{ column_tooltip }}</th>
                </tr>
              </thead>
              <tbody>
                {% for i, b in blocks %}
                  <tr data-fp-row>
                    <td>
                      <div class="row g-2">
                        <div class="col-md-4">
                          <input type="text" class="form-control" name="blocks[{{ i }}][key]" value="{{ b.key ?? '' }}" placeholder="key (e.g. price, manufacturer, option:14)"/>
                        </div>
                        <div class="col-md-8">
                          {% for language in languages %}
                            <div class="input-group mb-2">
                              <span class="input-group-text"><img src="language/{{ language.code }}/{{ language.code }}.png" alt="{{ language.name }}" title="{{ language.name }}"></span>
                              <input type="text" class="form-control" name="blocks[{{ i }}][label][{{ language.code }}]" value="{{ b.label[language.code] ?? '' }}" placeholder="{{ column_block }}"/>
                            </div>
                          {% endfor %}
                        </div>
                      </div>
                    </td>
                    <td>
                      <select class="form-select" name="blocks[{{ i }}][display]">
                        {% for opt in display_options %}
                          <option value="{{ opt.value }}" {% if b.display == opt.value %}selected{% endif %}>{{ opt.text }}</option>
                        {% endfor %}
                      </select>
                    </td>
                    <td class="text-center">
                      <input type="checkbox" class="form-check-input" name="blocks[{{ i }}][expanded]" value="1" {% if b.expanded %}checked{% endif %}/>
                      <div class="mt-2">
                        <input type="number" class="form-control form-control-sm" name="blocks[{{ i }}][sort_order]" value="{{ b.sort_order ?? (i * 10) }}" placeholder="sort" />
                      </div>
                    </td>
                    <td>
                      {% for language in languages %}
                        <div class="input-group mb-2">
                          <span class="input-group-text"><img src="language/{{ language.code }}/{{ language.code }}.png" alt="{{ language.name }}" title="{{ language.name }}"></span>
                          <textarea class="form-control" name="blocks[{{ i }}][tooltip][{{ language.code }}]" rows="2">{{ b.tooltip[language.code] ?? '' }}</textarea>
                        </div>
                      {% endfor %}

                      <div class="text-end">
                        <button type="button" class="btn btn-outline-danger btn-sm" data-fp-remove>Remove</button>
                      </div>
                    </td>
                  </tr>
                {% endfor %}

                <tr>
                  <td colspan="4">
                    <button type="button" class="btn btn-outline-primary" id="fp-add-row">Add block</button>
                    <div class="form-text mt-2">
                      Keys supported: <code>price</code>, <code>manufacturer</code>, <code>option:&lt;id&gt;</code>, <code>attribute:&lt;id&gt;</code>, <code>popular_queries</code>.
                      Legacy keys: <code>color</code>, <code>style</code>, <code>size</code>.
                    </div>
                  </td>
                </tr>
              </tbody>
            </table>
          </div>

          <hr/>

          <h3 class="mb-3">SEO-лендинги (FilterPro)</h3>
          <div class="alert alert-info">
            Здесь настраиваются «красивые» короткие URL для конкретных комбинаций фильтров (legacy FilterPro), а также H1/Title/Description/SEO-текст для RU/UA.
          </div>

          <div class="table-responsive">
            <table class="table table-bordered table-hover" id="fp-seo-landings">
              <thead>
                <tr>
                  <th style="width:110px">filter_id</th>
                  <th style="width:220px">keyword (slug)</th>
                  <th>url params</th>
                  <th style="width:160px">Действия</th>
                </tr>
              </thead>
              <tbody>
                {% for i, l in seo_landings %}
                  <tr data-fp-seo-row>
                    <td>
                      <input type="number" class="form-control" name="seo_landings[{{ i }}][filter_id]" value="{{ l.filter_id ?? '' }}" min="1" />
                    </td>
                    <td>
                      <input type="text" class="form-control" name="seo_landings[{{ i }}][keyword]" value="{{ l.keyword ?? '' }}" placeholder="krasivyj-url" />
                      <div class="form-text">Без /ru и без слешей в начале.</div>
                    </td>
                    <td>
                      <input type="text" class="form-control" name="seo_landings[{{ i }}][url]" value="{{ l.url ?? '' }}" placeholder="path=...&manufacturer=..." />
                      <div class="row mt-2 g-2">
                        <div class="col-md-6">
                          <div class="card">
                            <div class="card-header">RU</div>
                            <div class="card-body">
                              <input class="form-control mb-2" type="text" name="seo_landings[{{ i }}][lang][1][h1]" value="{{ l.lang[1].h1 ?? '' }}" placeholder="H1" />
                              <input class="form-control mb-2" type="text" name="seo_landings[{{ i }}][lang][1][title]" value="{{ l.lang[1].title ?? '' }}" placeholder="Title" />
                              <input class="form-control mb-2" type="text" name="seo_landings[{{ i }}][lang][1][meta_description]" value="{{ l.lang[1].meta_description ?? '' }}" placeholder="Meta description" />
                              <input class="form-control mb-2" type="text" name="seo_landings[{{ i }}][lang][1][meta_keywords]" value="{{ l.lang[1].meta_keywords ?? '' }}" placeholder="Meta keywords" />
                              <textarea class="form-control" rows="4" name="seo_landings[{{ i }}][lang][1][description]" placeholder="SEO текст (RU)">{{ l.lang[1].description ?? '' }}</textarea>
                            </div>
                          </div>
                        </div>
                        <div class="col-md-6">
                          <div class="card">
                            <div class="card-header">UA</div>
                            <div class="card-body">
                              <input class="form-control mb-2" type="text" name="seo_landings[{{ i }}][lang][4][h1]" value="{{ l.lang[4].h1 ?? '' }}" placeholder="H1" />
                              <input class="form-control mb-2" type="text" name="seo_landings[{{ i }}][lang][4][title]" value="{{ l.lang[4].title ?? '' }}" placeholder="Title" />
                              <input class="form-control mb-2" type="text" name="seo_landings[{{ i }}][lang][4][meta_description]" value="{{ l.lang[4].meta_description ?? '' }}" placeholder="Meta description" />
                              <input class="form-control mb-2" type="text" name="seo_landings[{{ i }}][lang][4][meta_keywords]" value="{{ l.lang[4].meta_keywords ?? '' }}" placeholder="Meta keywords" />
                              <textarea class="form-control" rows="4" name="seo_landings[{{ i }}][lang][4][description]" placeholder="SEO текст (UA)">{{ l.lang[4].description ?? '' }}</textarea>
                            </div>
                          </div>
                        </div>
                      </div>
                    </td>
                    <td class="text-end">
                      <button type="button" class="btn btn-outline-danger btn-sm" data-fp-seo-remove>Remove</button>
                    </td>
                  </tr>
                {% endfor %}

                <tr>
                  <td colspan="4">
                    <button type="button" class="btn btn-outline-primary" id="fp-seo-add">Add SEO landing</button>
                    <div class="form-text mt-2">filter_id должен совпадать с существующим landing’ом (или добавь новый filter_id вручную). keyword будет записан в seo_url как route=filter_id.</div>
                  </td>
                </tr>
              </tbody>
            </table>
          </div>

          <hr/>

          <h3 class="mb-3">{{ text_popular_queries|default('Popular queries') }}</h3>
          <div class="row mb-3">
            <label class="col-sm-2 col-form-label">{{ entry_popular_queries|default('Links list') }}</label>
            <div class="col-sm-10">
              {% for language in languages %}
                <div class="input-group mb-2">
                  <span class="input-group-text"><img src="language/{{ language.code }}/{{ language.code }}.png" alt="{{ language.name }}" title="{{ language.name }}"></span>
                  <textarea class="form-control" name="popular_queries[{{ language.code }}]" rows="6" placeholder="one per line: label|url">{{ popular_queries[language.code] ?? '' }}</textarea>
                </div>
              {% endfor %}
              <div class="form-text">
                One per line: <code>label|url</code>. Example: <code>купить мужское белье|https://example.com</code>
              </div>
            </div>
          </div>

        </form>
      </div>
    </div>
  </div>
</div>
<script>
  (function() {
    function matches(el, selector) {
      if (!el) return false;
      var fn = el.matches || el.msMatchesSelector || el.webkitMatchesSelector;
      return fn ? fn.call(el, selector) : false;
    }

    function closest(el, selector) {
      while (el && el.nodeType === 1) {
        if (matches(el, selector)) return el;
        el = el.parentElement;
      }
      return null;
    }

    function nextIndex() {
      var rows = document.querySelectorAll('tr[data-fp-row]');
      return rows.length;
    }

    function rowTemplate(i) {
      // Note: tooltips are per-language
      var languagesRaw = {{ languages|json_encode|raw }};
      var languages = Array.isArray(languagesRaw) ? languagesRaw : Object.values(languagesRaw || {});
      var tooltipHtml = '';
      languages.forEach(function(lang) {
        tooltipHtml +=
          '<div class="input-group mb-2">' +
            '<span class="input-group-text"><img src="language/' + lang.code + '/' + lang.code + '.png" alt="' + lang.name + '" title="' + lang.name + '"></span>' +
            '<textarea class="form-control" name="blocks[' + i + '][tooltip][' + lang.code + ']" rows="2"></textarea>' +
          '</div>';
      });

      return (
        '<tr data-fp-row>' +
          '<td>' +
            '<div class="row g-2">' +
              '<div class="col-md-4">' +
                '<input type="text" class="form-control" name="blocks[' + i + '][key]" value="" placeholder="key (e.g. price, manufacturer, option:14)"/>' +
              '</div>' +
              '<div class="col-md-8">' +
                (function(){
                  var html='';
                  languages.forEach(function(lang){
                    html += '<div class="input-group mb-2">' +
                      '<span class="input-group-text"><img src="language/' + lang.code + '/' + lang.code + '.png" alt="' + lang.name + '" title="' + lang.name + '"></span>' +
                      '<input type="text" class="form-control" name="blocks[' + i + '][label][' + lang.code + ']" value="" placeholder="{{ column_block }}"/>' +
                    '</div>';
                  });
                  return html;
                })() +
              '</div>' +
            '</div>' +
          '</td>' +
          '<td>' +
            '<select class="form-select" name="blocks[' + i + '][display]">' +
              {% for opt in display_options %}
                '<option value="{{ opt.value }}">{{ opt.text|e('js') }}</option>' +
              {% endfor %}
            '</select>' +
          '</td>' +
          '<td class="text-center">' +
            '<input type="checkbox" class="form-check-input" name="blocks[' + i + '][expanded]" value="1" checked />' +
            '<div class="mt-2">' +
              '<input type="number" class="form-control form-control-sm" name="blocks[' + i + '][sort_order]" value="' + ((i + 1) * 10) + '" placeholder="sort" />' +
            '</div>' +
          '</td>' +
          '<td>' +
            tooltipHtml +
            '<div class="text-end">' +
              '<button type="button" class="btn btn-outline-danger btn-sm" data-fp-remove>Remove</button>' +
            '</div>' +
          '</td>' +
        '</tr>'
      );
    }

    function nextSeoIndex() {
      var rows = document.querySelectorAll('tr[data-fp-seo-row]');
      return rows.length;
    }

    function seoRowTemplate(i) {
      return (
        '<tr data-fp-seo-row>' +
          '<td><input type="number" class="form-control" name="seo_landings[' + i + '][filter_id]" value="" min="1" /></td>' +
          '<td>' +
            '<input type="text" class="form-control" name="seo_landings[' + i + '][keyword]" value="" placeholder="krasivyj-url" />' +
            '<div class="form-text">Без /ru и без слешей в начале.</div>' +
          '</td>' +
          '<td>' +
            '<input type="text" class="form-control" name="seo_landings[' + i + '][url]" value="" placeholder="path=...&manufacturer=..." />' +
            '<div class="row mt-2 g-2">' +
              '<div class="col-md-6">' +
                '<div class="card"><div class="card-header">RU</div><div class="card-body">' +
                  '<input class="form-control mb-2" type="text" name="seo_landings[' + i + '][lang][1][h1]" value="" placeholder="H1" />' +
                  '<input class="form-control mb-2" type="text" name="seo_landings[' + i + '][lang][1][title]" value="" placeholder="Title" />' +
                  '<input class="form-control mb-2" type="text" name="seo_landings[' + i + '][lang][1][meta_description]" value="" placeholder="Meta description" />' +
                  '<input class="form-control mb-2" type="text" name="seo_landings[' + i + '][lang][1][meta_keywords]" value="" placeholder="Meta keywords" />' +
                  '<textarea class="form-control" rows="4" name="seo_landings[' + i + '][lang][1][description]" placeholder="SEO текст (RU)"></textarea>' +
                '</div></div>' +
              '</div>' +
              '<div class="col-md-6">' +
                '<div class="card"><div class="card-header">UA</div><div class="card-body">' +
                  '<input class="form-control mb-2" type="text" name="seo_landings[' + i + '][lang][4][h1]" value="" placeholder="H1" />' +
                  '<input class="form-control mb-2" type="text" name="seo_landings[' + i + '][lang][4][title]" value="" placeholder="Title" />' +
                  '<input class="form-control mb-2" type="text" name="seo_landings[' + i + '][lang][4][meta_description]" value="" placeholder="Meta description" />' +
                  '<input class="form-control mb-2" type="text" name="seo_landings[' + i + '][lang][4][meta_keywords]" value="" placeholder="Meta keywords" />' +
                  '<textarea class="form-control" rows="4" name="seo_landings[' + i + '][lang][4][description]" placeholder="SEO текст (UA)"></textarea>' +
                '</div></div>' +
              '</div>' +
            '</div>' +
          '</td>' +
          '<td class="text-end"><button type="button" class="btn btn-outline-danger btn-sm" data-fp-seo-remove>Remove</button></td>' +
        '</tr>'
      );
    }

    document.addEventListener('click', function(e) {
      var t = e.target;

      // Add block
      var addBtn = closest(t, '#fp-add-row');
      if (addBtn) {
        var i = nextIndex();
        var addRowTr = closest(addBtn, 'tr');
        if (addRowTr) {
          addRowTr.insertAdjacentHTML('beforebegin', rowTemplate(i));
        }
        return;
      }

      // Remove block
      var btn = closest(t, '[data-fp-remove]');
      if (btn) {
        var tr = closest(btn, 'tr');
        if (tr) tr.remove();
        return;
      }

      // Add SEO landing
      var seoAdd = closest(t, '#fp-seo-add');
      if (seoAdd) {
        var i2 = nextSeoIndex();
        var addRowTr2 = closest(seoAdd, 'tr');
        if (addRowTr2) {
          addRowTr2.insertAdjacentHTML('beforebegin', seoRowTemplate(i2));
        }
        return;
      }

      // Remove SEO landing
      var seoRm = closest(t, '[data-fp-seo-remove]');
      if (seoRm) {
        var tr2 = closest(seoRm, 'tr');
        if (tr2) tr2.remove();
        return;
      }
    });
  })();
</script>

{{ footer }}
